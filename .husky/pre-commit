#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
npx lint-staged --allow-empty

# ShiftAI Pre-commit Hook - Detect and strip AI markers
# Non-blocking: skip gracefully if token is not configured
npx @deriv-com/shiftai-cli hook pre-commit || echo "ShiftAI hook skipped (token not configured)"